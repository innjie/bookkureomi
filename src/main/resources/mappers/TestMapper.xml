<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.inyoon.bookkureomi.test.TestMapper">
	<select id="findUser" parameterType="String" resultType="com.inyoon.bookkureomi.test.Test">
        SELECT id, phone FROM users where id = #{id}
    </select>
</mapper>