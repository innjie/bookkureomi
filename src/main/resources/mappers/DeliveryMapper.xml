<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.inyoon.bookkureomi.delivery.DeliveryMapper">
	<select id="getDelivery" parameterType="integer" resultType="com.inyoon.bookkureomi.domain.Delivery">
        SELECT 
			orderNo, company, waybill
		FROM 
			Delivery <!-- Orders 추가 -->
		where
			orderNo = #{orderNo}
    </select>
    
    <insert id="addDelivery" parameterType="com.inyoon.bookkureomi.domain.Delivery">
    	Insert into Delivery (orderNo, company, waybill)
    	Values (#{order.orderNo}, #{company}, #{waybill})
    </insert>
    
    <update id="updateDelivery">
    	UPDATE Delivery
    	SET
            company = #{company},
            waybill = #{waybill}
        WHERE 
        	orderNo = #{order.orderNo}
    </update>
    
    <update id="updateSale"> <!--  parameterType="hashmap" -->
        UPDATE Sale
        SET
            salePrice = #{salePrice},
            image = #{image},
            info = #{info}
        WHERE 
        	saleNo = #{saleNo}
    </update>
</mapper>